import type { Metadata } from "next";
export const metadata: Metadata = { title:"Jobs at Mwenza Kenya | Build work that matters", description:"Explore service professional, operations, customer support and growth jobs with Mwenza across Kenya." };
export default function JobsLayout({children}:{children:React.ReactNode}) { return children; }
