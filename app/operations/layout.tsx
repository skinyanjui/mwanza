import type { Metadata } from "next";
export const metadata: Metadata={title:"Mwenza Operations",description:"Dispatch, provider, customer and business service operations workspace."};
export default function OperationsLayout({children}:{children:React.ReactNode}){return children}

