import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Book a Mwenza Service | Mwenza Kenya",
  description: "Choose a Mwenza service, location and time, then review your request before confirming.",
};

export default function BookLayout({children}:{children:React.ReactNode}) { return children; }
