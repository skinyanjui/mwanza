import type { Metadata } from "next";
export const metadata: Metadata={title:"Account access | Mwenza Kenya",description:"Sign in or create a secure Mwenza account for home and business services."};
export default function AccountLayout({children}:{children:React.ReactNode}){return children}
